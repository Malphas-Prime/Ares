<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ares Prep Utility"
        Height="520"
        Width="820"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#f0f0f0"
        FontFamily="Segoe UI"
        FontSize="12">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="4,0,4,8">
            <TextBlock Text="Ares Prep Utility"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="#202020" />
            <TextBlock Text="Select the tasks you want to run on this machine."
                       Margin="0,2,0,0"
                       Foreground="#505050" />

            <!-- Machine info row -->
            <Border Margin="0,10,0,0"
                    Background="White"
                    BorderBrush="#c0c0c0"
                    BorderThickness="1"
                    Padding="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Vertical">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <TextBlock Text="Hostname:" Width="90" Foreground="#505050"/>
                            <TextBlock Name="HostNameText" Text="-" FontWeight="SemiBold" Foreground="#202020"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <TextBlock Text="Active IP:" Width="90" Foreground="#505050"/>
                            <TextBlock Name="ActiveIpText" Text="-" FontWeight="SemiBold" Foreground="#202020"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="N-able AV:" Width="90" Foreground="#505050"/>
                            <TextBlock Name="ManagedAvText" Text="-" FontWeight="SemiBold" Foreground="#202020"/>
                        </StackPanel>
                    </StackPanel>

                    <Button Grid.Column="2"
                            Name="RefreshInfoButton"
                            Content="Refresh"
                            Width="90"
                            Padding="8,4"
                            Margin="10,0,0,0"
                            VerticalAlignment="Top"/>
                </Grid>
            </Border>
        </StackPanel>

        <!-- Main list -->
        <Border Grid.Row="1"
                Margin="0,0,0,6"
                BorderBrush="#c0c0c0"
                BorderThickness="1"
                Background="White">
            <ListView Name="TaskList"
                      Margin="0"
                      BorderThickness="0">
                <ListView.View>
                    <GridView>
                        <!-- Checkbox column -->
                        <GridViewColumn Width="40">
                            <GridViewColumn.Header>
                                <TextBlock Text="" />
                            </GridViewColumn.Header>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected}"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- Task name -->
                        <GridViewColumn Header="Task" Width="210">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- Description -->
                        <GridViewColumn Header="Description" Width="420">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Description}"
                                               TextWrapping="Wrap" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- Status -->
                        <GridViewColumn Header="Status" Width="90">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Status}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <!-- Footer -->
        <DockPanel Grid.Row="2">
            <TextBlock Name="StatusText"
                       Margin="2,0,0,0"
                       VerticalAlignment="Center"
                       Foreground="#505050"
                       DockPanel.Dock="Left" />
            <StackPanel Orientation="Horizontal"
                        DockPanel.Dock="Right"
                        HorizontalAlignment="Right">
                <Button Name="RunButton"
                        Content="Run Selected"
                        Width="120"
                        Margin="0,0,8,0"
                        Padding="8,4" />
                <Button Name="CloseButton"
                        Content="Close"
                        Width="80"
                        Padding="8,4" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
