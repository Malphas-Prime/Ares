<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ares Prep Utility"
        Height="520"
        Width="820"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#f0f0f0"
        FontFamily="Segoe UI"
        FontSize="12">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="4,0,4,8">
            <TextBlock Text="Ares Prep Utility"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="#202020" />
            <TextBlock Text="Select the tasks you want to run on this machine."
                       Margin="0,2,0,6"
                       Foreground="#505050" />

            <!-- Machine Info Bar -->
            <Border Background="White"
                    BorderBrush="#c0c0c0"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="10"
                    Margin="0,0,0,2">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Hostname: " FontWeight="SemiBold" Foreground="#303030"/>
                        <TextBlock Name="HostNameText" Text="-" Foreground="#202020" Margin="0,0,14,0"/>

                        <TextBlock Text="Active IPv4: " FontWeight="SemiBold" Foreground="#303030"/>
                        <TextBlock Name="ActiveIpText" Text="-" Foreground="#202020" Margin="0,0,14,0"/>

                        <TextBlock Text="N-able Managed AV: " FontWeight="SemiBold" Foreground="#303030"/>
                        <TextBlock Name="ManagedAvText" Text="-" Foreground="#202020"/>
                    </StackPanel>

                    <Button Grid.Column="1"
                            Name="RefreshInfoButton"
                            Content="Refresh"
                            Width="90"
                            Padding="8,4"
                            Margin="10,0,0,0"
                            VerticalAlignment="Center"/>
                </Grid>
            </Border>
        </StackPanel>

        <!-- Main list -->
        <Border Grid.Row="1"
                Margin="0,0,0,6"
                BorderBrush="#c0c0c0"
                BorderThickness="1"
                Background="White">
            <ListView Name="TaskList"
                      Margin="0"
                      BorderThickness="0">
                <ListView.View>
                    <GridView>
                        <!-- Checkbox column -->
                        <GridViewColumn Width="40">
                            <GridViewColumn.Header>
                                <TextBlock Text="" />
                            </GridViewColumn.Header>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected}"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- Task name -->
                        <GridViewColumn Header="Task" Width="210">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- Description -->
                        <GridViewColumn Header="Description" Width="420">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Description}"
                                               TextWrapping="Wrap" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- Status -->
                        <GridViewColumn Header="Status" Width="90">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Status}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <!-- Footer -->
        <DockPanel Grid.Row="2">
            <TextBlock Name="StatusText"
                       Margin="2,0,0,0"
                       VerticalAlignment="Center"
                       Foreground="#505050"
                       DockPanel.Dock="Left" />
            <StackPanel Orientation="Horizontal"
                        DockPanel.Dock="Right"
                        HorizontalAlignment="Right">
                <Button Name="RunButton"
                        Content="Run Selected"
                        Width="120"
                        Margin="0,0,8,0"
                        Padding="8,4" />
                <Button Name="CloseButton"
                        Content="Close"
                        Width="80"
                        Padding="8,4" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
